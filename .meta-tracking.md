# Meta-Tracking: code:recursive-spec IMPL.md

## Friction
- hickory-resolver 0.25: error module now private, use `hickory_resolver::ResolveError` directly
- hickory-resolver 0.25: NXDOMAIN via `proto_err.is_nx_domain()` not `ResolveErrorKind::NoRecordsFound`
- publicsuffix 2: no `List::fetch()` or `List::empty()`, use `List::new()` instead
- publicsuffix 2: requires `use publicsuffix::Psl` trait import for `.domain()` method
- publicsuffix 2: domain() returns opaque type, needs explicit type annotation in closure
- ring: requires `use ring::signature::KeyPair` trait for `public_key()` method
- clippy: `from_str()` method name conflicts with std trait, rename to `parse()`
- clippy: `map_or(false, ...)` → `is_some_and(...)` (Rust 1.80+)
- clippy: prefer `#[derive(Default)]` with `#[default]` attribute over manual impl
- MSRV: LazyLock stable in 1.80, not 1.75

## Waste

## Insight
- Spec learnings section was helpful but some were outdated/inaccurate for 0.25
- ring API is stable, hickory-resolver API keeps changing

## Deviation
- Bumped MSRV from 1.75 to 1.80 for LazyLock stability

## Skipped
- versioned working copy (impl/v{n}/) - single session, velocity mode
